#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main(){

	setlocale(LC_ALL, "portuguese");

	int num = 0;
	printf("Informe um numero: \n ");
	scanf("%i", &num);
	if (num%2 == 0) {
		printf("Numero é par."); }
	else{
		printf("O numero é impar."); }
	


}
